// src/features/referral/constants/field.ts

// このファイルは domain-config スクリプトによって自動生成されています。
// 手動での編集は変更が上書きされる可能性があるため推奨されません。

export const ReferralStatusOptions = [
  { value: "active", label: "有効" },
  { value: "cancelled", label: "取消" }
] as const;
