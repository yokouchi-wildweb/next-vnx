// src/engine/features/Dialogue/components/MessageList/defaults.ts

import type { MessageListLayout } from "./types"

/** MessageList のデフォルト配置設定 */
export const defaultMessageListLayout: MessageListLayout = {
  widthPercent: 40,
  minWidth: 300,
  maxWidth: 600,
  fadeTop: 20,
  fadeBottom: 90,
  paddingBottomPercent: 5,
  gap: 15,
}
